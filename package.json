{
  "name": "possible-words",
  "version": "1.0.0",
  "description": "A function that accepts a single string as input and returns a list of English words that can be created using some combination of the letters in the input string.",
  "scripts": {
    "test": "jest"
  },
  "author": "gotno",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "npm": "10.x",
    "node": "21.x"
  }
}
